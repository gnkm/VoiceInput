pre-commit:
  parallel: true
  commands:
    pretty:
      glob: "*.dart"
      run: dart format .
    linter:
      glob: "*.dart"
      run: flutter analyze
    tests:
      glob: "*.dart"
      run: flutter test
